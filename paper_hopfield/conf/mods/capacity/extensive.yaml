# @package _global_

capacity:
  interrupt: False
  threshold: 0.95 #when training will STOP, basically unusable
  epsilon: 0.95 #when training will count as SUCCESSFUL
  n_checkpoints: 15
  start: 0.1
  step: 0.1
  stop: 2.1

params:
  exp_name : 'Capacity'

storage:
  capacity: True